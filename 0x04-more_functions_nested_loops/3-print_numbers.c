#include "main.h"

/**
 * print_numbers - prints numbers from 0 to 9
 * Return: no return
 */
void print_numbers(void)
{
	int lm;

	for (lm = 48; lm < 58; lm++)
	{
		_putchar(lm);
	}
	_putchar('\n');
}
